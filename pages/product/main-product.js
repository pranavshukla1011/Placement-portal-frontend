/*
* the dynamic review structure can still be used even-if a page having static path is used.
* in this case the static path is converted to dynamic path and key is stored in router.query
* */
const MainProduct = () => {
    return <div>
        Main Product
    </div>;
};

export default MainProduct;